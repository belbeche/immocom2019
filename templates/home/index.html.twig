{% extends 'base.html.twig' %}

{% block title %}Notasphere v1{% endblock %}

{% block body %}

<div class="jumbotron">
  <div class="jumbotron container-fluide">
    <div class="row">
      <div class="col-md-8" style="display:flex;justify-content:flex-end;"> 
        <div> Autres elements </div>
      </div>
      <div class="jumbotron container-fluide" style="margin-top: -100px;">
        <div class="container row align-items-end ">
          <div class="col" style="display:inline-flex;"> 
          </div>
          {{ form_start(form) }}
            <div class="col" style="display:flex;"> 
            <label> Je veux : </label>
              <div class="form-group" style="margin-left: 10px;">{{ form_row(form.location, {'label': 'Louer'}) }} </div>
              <div class="form-group" style="margin-left: 10px;">{{ form_row(form.Achat, {'label': 'Acheter'}) }} </div>
            </div>
            <div class="col" style="display:flex;"> 
            <label> Type : </label>
              
            </div>
            <div class="col"> 
            {{ form_row(form.minSurface) }}
            </div>
            <div class="col"> 
            {{ form_row(form.maxPrice) }}
            </div>
            
          {{ form_rest(form) }}
          {{ form_end(form) }}
        </div>
      </div>
    </div>
   <p>Au fil des ans, nous avons aqcuis un savoir-faire et un savoir-être</p>
  <hr class="my-4">
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">En savoir plus</a>
  </p>
</div>

{% endblock %}
